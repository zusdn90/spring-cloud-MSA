server:
  port: 8761
spring:
  application:
    name: eureka-discovery
  profiles:
    active: local
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: false